// TEMPLATE — values substituted at container startup via envsubst.
window.RUNTIME_ENV = {
  API_BASE_URL: '${API_BASE_URL}',
  OIDC_AUTHORITY: '${OIDC_AUTHORITY}',
  OIDC_CLIENT_ID: '${OIDC_CLIENT_ID}',
};
